checklist
=====================================================================

To install::

    pip install https://github.com/meshantz/django-checklist.git

Then add ``checklist`` to your installed apps. ::

    INSTALLED_APPS = (
        ...
        'checklist',
        ...
    )

